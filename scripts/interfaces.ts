export interface CSVData {
    address: string;
    poolAddress: string;
    count: number;
    network: string;
}

export interface CSVDataExt {
    address: string;
    routerType: string;
    poolAddress: string;
    factoryType: string;
    count: number;
    network: string;
}